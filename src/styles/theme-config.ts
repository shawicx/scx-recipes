// 基于当前项目的设计系统配置
// 由于HeroUI可能没有createTheme函数，我们将使用CSS变量定义主题
// 所有主题相关的CSS变量都定义在globals.css的@theme块中

// 这个文件保留作为空的导出，以防其他组件引用了这些导出
export const theme = {};
export const darkTheme = {};
export const lightTheme = {};